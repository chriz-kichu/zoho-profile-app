## Note

The `.env` file is not included in this repository because it contains my local database details and Google OAuth credentials.

I have added `.env` to `.gitignore` to keep these details private.

To run this project, create a `.env` file in the project folder and add your own database and Google OAuth configuration.
